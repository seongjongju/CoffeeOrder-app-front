import React from 'react';
import {NaviContainer} from '../styled/GlobalStyled';

const NavigationBar = () => {
    return (
        <NaviContainer>
    
        </NaviContainer>
    );
};

export default NavigationBar;